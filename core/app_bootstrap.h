#ifndef APP_BOOTSTRAP_H
#define APP_BOOTSTRAP_H

// Bootstrap all built-in apps into the filesystem
// Creates .app bundles with Info.plist for each built-in app
void app_bootstrap_init(void);

#endif
